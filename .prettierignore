node_modules
dist
build
coverage
.expo
.next
*.tsbuildinfo
pnpm-lock.yaml

# Expo regenerates apps/web/tsconfig.json on every `expo start` (it adds
# .expo/types/** and expo-env.d.ts to the include array). Letting prettier
# reformat it just causes a fight that can't be won.
apps/web/tsconfig.json
