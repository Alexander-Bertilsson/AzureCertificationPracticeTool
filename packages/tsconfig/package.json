{
  "name": "@acpt/tsconfig",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript configurations extended by every package and app in the monorepo.",
  "files": ["base.json", "node.json", "react-native.json"]
}
